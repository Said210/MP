<!--<h1>Welcome#index</h1>
<p>Find me in app/views/welcome/index.html.erb</p>

 <% @u.each do |i| %>
	<%= i.username %>
<%end%>

<br>

<div ng-controller="Albums">
		
		<form action="#">
			<input type="text" id="LookFor">
			<input type="button" ng-click="buscar()">
		</form>
		
		<div ng-repeat="i in Spotify_albums.items">
			<img src="{{i.images[0].url}}" width="50px" alt="">
			{{i.name}} <br>
			<span class="link" ng-click="showTracks(i.id)">Mostrar e.e</span>
		</div>
		
		<div ng-repeat="j in Spotify_album_tracks">
			{{j.name}} <br>
			<span class="link" ng-click="playPreview(j.preview_url)">Play</span>
		</div>

	</div>
-->

<script type="text/javascript">
	
	$(window).scroll(function (event) {
	    $(".section").each(function(){
	    	doSc($(this),82);
	    });
	    $(".middle").each(function(){
	    	doSc($(this),1);
	    });
	});
	function doSc(elem, q){
		var scroll = $(window).scrollTop();
		var space=$(elem).data("place");
		var speed=$(elem).data("speed");
		console.log(speed);
		if(speed==undefined){speed=q;}else{}
		if(space<2){
			var s="calc("+100*space+"% + ";
			var s2=(speed-scroll)/space;
			s=s+s2+"px)";
			$(elem).css("top",s);
		}else{
			var s="calc("+100*space*.75+"% + ";
			var s2=(speed-scroll)/space;
			
			s=s+s2+"px)";
			$(elem).css("top",s);
		}
	}

</script>

<div class="section section1" data-place="0">
	<div id="insider">
		<h1>Musicpoint</h1>
		<h3>Share your good taste</h3>
	</div>
</div>
<div class="middle middle1" data-place=".95">
	<h1>Conoce nuevas personas</h1>
	<h3>Conoce nuevas personas y sigue en contacto con los viejos conocidos descubriendo los gustos de las personas &gr;3</h3>
</div>
<div class="section2 section" data-place="1" data-speed="10">
	<div id="insider">
		<h1>Musicpoint2</h1>
		<h3>Share your good taste</h3>
	</div>
</div>
<div class="section3 section" data-place="2" data-speed="100">
	<div id="insider">
		<h1>Unete Ahora</h1>
		<button>¡Registrarme!</button>
	</div>
</div>
<div class="middle middle1" data-place="2.97" data-speed="90" style="min-height:100px">
	<sub>Musicpoint by Breakpoint at Notion Engineering</sub>
</div>